name: tutorial

python_env: python_env.yaml

entry_points:
  main:
    parameters:
      n_estimators: {type: int, default: 100}
      max_depth: {type: int, default: 6}
      max_features: {type: int, default: 3}
    command: "python train.py {n_estimators} {max_depth} {max_features}"
